<view class="playing-status">
  
  <progress color="#dadada" stroke-width="2">
    <text class="playstate" style="left:{{percent}}%"></text>
    <text class="dpstate" style="width:{{percent}}%"></text>
    <slider bindchange="museek" color="#d33a31" left-icon="cancel" value="{{percent}}"/>
  </progress>
  
  <view class="btns">
    <view wx:key="index" wx:for="{{items}}">
      <image bindtap="player" data-name="{{item.name}}" wx:if="{{item.name === 'stop'}}" src="{{playing ? stopUrl : playUrl}}" />
      <image bindtap="player" data-name="{{item.name}}" wx:else src="{{item.img}}" />
    </view>
    <view bindtap="playInfo">
      <image src="{{songInfo.al.picUrl}}" />
      <text>{{songInfo.name}}</text>
    </view>
  </view>
</view>
